# Shell scripts must stay LF — a CRLF supervisor.sh baked into a Docker image
# fails at container boot (busybox sh rejects \r). Windows checkouts with
# core.autocrlf=true otherwise corrupt them silently.
*.sh text eol=lf
